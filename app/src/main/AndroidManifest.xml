<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dani.alarmpro1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AjustesActivity" />
        <activity android:name=".ConfiguracionAlarmaActivity" />
        <activity android:name=".ConfiguracionRepeticionAlarmaActivity" />
        <activity android:name=".ConfiguracionRingtoneAlarmaActivity" />
        <activity android:name=".ConfiguracionPosponerActivity" />
        <activity android:name=".ConfiguracionSkipActivity" />
        <activity android:name=".ConfiguracionIteracionActivity"></activity>
    </application>

</manifest>